package com.wemb.spp.fwk.util;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class FasooDrmDec {

	private static Logger log = LoggerFactory.getLogger(FasooDrmDec.class);

	public static void main(String[] args) {

		String srcFile = "D:\\참고/(총무팀.이승중)0217_2025년 고정투자_기초자료.xlsx";
		
		boolean isSuccess = DrmUtil.decFasooDrmFile(srcFile, srcFile); //복호화 된 문서 FullPath + FileName
		log.debug("isSuccess:" + isSuccess);
	}
}